# Prisma / SQLite (swap to a Postgres URL for a real deployment)
DATABASE_URL="file:./dev.db"

# Session cookie signing secret - generate with: openssl rand -base64 32
SESSION_SECRET=""

# Groq API key used to power agent replies (https://console.groq.com)
GROQ_API_KEY=""
